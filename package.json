{
  "private": true,
  "scripts": {
    "build": "yarn clean && parcel build index.html",
    "clean": "rm -rf dist",
    "dev": "yarn clean && parcel index.html"
  },
  "dependencies": {
    "three": "^0.99.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.10.3"
  }
}
